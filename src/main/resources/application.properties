# =============================================================================
# COMMON (all environments)
# =============================================================================
quarkus.banner.enabled=false

# -----------------------------------------------------------------------------
# Observability
# -----------------------------------------------------------------------------

# Metrics Configuration
quarkus.micrometer.enabled=true
quarkus.micrometer.export.prometheus.enabled=${quarkus.micrometer.enabled}
quarkus.micrometer.export.prometheus.path=/q/metrics

# Health Checks
quarkus.smallrye-health.enabled=${quarkus.micrometer.enabled}
quarkus.smallrye-health.root-path=/q/health

# Enhanced Logging
quarkus.log.level=${LOG_LEVEL:INFO}
quarkus.log.category."io.agroal".min-level=TRACE
quarkus.log.category."io.agroal".level=DEBUG
quarkus.log.category."io.quarkus.http.access-log".level=INFO

# Application logging - controls com.elicitsoftware.* (login/auth debug statements, etc.)
quarkus.log.category."com.elicitsoftware".min-level=DEBUG
quarkus.log.category."com.elicitsoftware".level=${ELICIT_LOG_LEVEL:INFO}

# Database Telemetry
quarkus.datasource.jdbc.telemetry=true

# Hibernate Statistics for Performance Monitoring
quarkus.hibernate-orm.statistics=true
quarkus.hibernate-orm.log.sql=false
quarkus.hibernate-orm.log.bind-parameters=false

# HTTP Access Logging
quarkus.http.access-log.enabled=true
quarkus.http.access-log.pattern=%h %l %u %t "%r" %s %b "%{i,Referer}" "%{i,User-Agent}" %D

# Micrometer Binders - Enable comprehensive metrics collection
quarkus.micrometer.binder.jvm=true
quarkus.micrometer.binder.system=true
quarkus.micrometer.binder.http-server.enabled=true
quarkus.micrometer.binder.http-client.enabled=true

# HTTP Server Metrics Configuration
quarkus.micrometer.binder.http-server.max-uri-tags=100
quarkus.micrometer.binder.http-server.ignore-patterns=/q/.*

# Override any potential JSON logging from parent configs
quarkus.log.console.json=false

# -----------------------------------------------------------------------------
# OpenTelemetry
# -----------------------------------------------------------------------------

# Enable OpenTelemetry
quarkus.otel.enabled=true

# Application/Service Name for OpenTelemetry Resource Attributes
quarkus.otel.service.name=${quarkus.application.name}

# Tracing Configuration (enabled by default)
quarkus.otel.traces.enabled=true
quarkus.otel.traces.sampler=parentbased_always_on
quarkus.otel.traces.suppress-non-application-uris=true

# Metrics Configuration (experimental - enable if needed)
# quarkus.otel.metrics.enabled=true
# quarkus.otel.metric.export.interval=60s

# Logs Configuration (experimental - enable if needed)
# quarkus.otel.logs.enabled=true

# OTLP Exporter Configuration
# Default endpoint is http://localhost:4317 for gRPC
quarkus.otel.exporter.otlp.endpoint=http://localhost:4317
quarkus.otel.exporter.otlp.protocol=grpc

# Optional: Authentication headers for OTLP endpoint
# quarkus.otel.exporter.otlp.headers=authorization=Bearer <token>

# Optional: Compression (gzip or none)
# quarkus.otel.exporter.otlp.compression=gzip

# Add trace context to console logs
quarkus.log.console.format=%d{HH:mm:ss} %-5p traceId=%X{traceId}, parentId=%X{parentId}, spanId=%X{spanId}, sampled=%X{sampled} [%c{2.}] (%t) %s%e%n

# Instrumentation Configuration
quarkus.otel.instrument.vertx-http=true
quarkus.otel.instrument.vertx-event-bus=true
quarkus.otel.instrument.vertx-sql-client=true
quarkus.otel.instrument.rest=true
quarkus.otel.instrument.resteasy=true

# Resource Attributes
quarkus.otel.resource.attributes=deployment.environment=${DEPLOYMENT_ENV:dev}

# Propagators (W3C Trace Context and Baggage by default)
quarkus.otel.propagators=tracecontext,baggage

# Span Limits
quarkus.otel.span.attribute.count.limit=128
quarkus.otel.span.event.count.limit=128
quarkus.otel.span.link.count.limit=128

# Batch Span Processor Configuration
quarkus.otel.bsp.schedule.delay=5s
quarkus.otel.bsp.max.queue.size=2048
quarkus.otel.bsp.max.export.batch.size=512
quarkus.otel.bsp.export.timeout=30s

# -----------------------------------------------------------------------------
# Application / metrics
# -----------------------------------------------------------------------------

# Application Name for Metrics
quarkus.application.name=elicit-fhhs

# Database Metrics
quarkus.datasource.metrics.enabled=${quarkus.micrometer.enabled}

# FHHS-specific metrics
fhhs.metrics.report.generation.enabled=${quarkus.micrometer.enabled}

# -----------------------------------------------------------------------------
# Datasource (default: prod-shaped, pointing at the `db` service host)
# -----------------------------------------------------------------------------
quarkus.datasource.db-kind=postgresql
quarkus.datasource.db-version=17
quarkus.datasource.jdbc.url=jdbc:postgresql://db:5432/survey?connectTimeout=30&socketTimeout=600&tcpKeepAlive=true&tcpKeepAlivesIdle=60&tcpKeepAlivesInterval=10&tcpKeepAlivesCount=60
quarkus.datasource.jdbc.min-size=5
quarkus.datasource.jdbc.validation-query-sql=SELECT 1
quarkus.datasource.jdbc.leak-detection-interval=10m
quarkus.datasource.jdbc.metrics.enabled=${quarkus.micrometer.enabled}
quarkus.datasource.username=survey_user
quarkus.datasource.password=SURVEYPW

# Datasource owner
quarkus.datasource.owner.db-kind=postgresql
quarkus.datasource.owner.db-version=17
quarkus.datasource.owner.jdbc.url=jdbc:postgresql://db:5432/survey?connectTimeout=30&socketTimeout=600&tcpKeepAlive=true&tcpKeepAlivesIdle=60&tcpKeepAlivesInterval=10&tcpKeepAlivesCount=60
quarkus.datasource.owner.jdbc.validation-query-sql=SELECT 1
quarkus.datasource.owner.jdbc.leak-detection-interval=10m
quarkus.datasource.owner.username=elicit_owner
quarkus.datasource.owner.password=SURVEYPW
quarkus.datasource.owner.jdbc.transaction-isolation-level=serializable

# Flyway minimal config properties
# migrate-at-start is deliberately false: Quarkus resolves classpath: migration locations
# at build time, baked into the generated FlywayRecorder bytecode, so a runtime location
# choice (greenfield db/migration vs. brownfield db/migration-v3) can't be expressed through
# Quarkus's own Flyway integration. com.elicitsoftware.flyway.ManualSchemaMigrator runs Flyway
# manually against an independent Flyway.configure() instance instead — see its header comment.
quarkus.flyway.owner.migrate-at-start=false
quarkus.flyway.owner.baseline-on-migrate=true
quarkus.flyway.owner.schemas=survey
quarkus.flyway.owner.validate-on-migrate=true
quarkus.flyway.owner.baseline-version=0
quarkus.flyway.owner.baseline-description=Empty Database
quarkus.flyway.owner.connect-retries=10
quarkus.flyway.owner.table=flyway_fhhs_history
quarkus.flyway.owner.placeholders.survey_user=survey_user
quarkus.flyway.owner.placeholders.surveyadmin_user=surveyadmin_user
quarkus.flyway.owner.placeholders.surveyreport_user=surveyreport_user
quarkus.flyway.owner.locations=db/migration

# -----------------------------------------------------------------------------
# Container image
# -----------------------------------------------------------------------------
quarkus.container-image.tag=latest
quarkus.container-image.registry=docker.io
quarkus.container-image.group=elicitsoftware
quarkus.container-image.name=fhhs
quarkus.container-image.build=true

# -----------------------------------------------------------------------------
# SFTP
# -----------------------------------------------------------------------------
# The service supports both password and SSH key authentication.
# If both are configured, SSH key authentication takes precedence.
# Configure either password OR privateKey, not both.
# Set family.history.sftp.enabled=false to disable the SFTP upload feature entirely
# (skips the startup connection test, report generation/upload, and the retry job).
# The Family History Survey this application serves (UC-005). FHHS is specific to that survey,
# so the readiness probe reports down and the report endpoints refuse until a survey with this
# key exists. Import FHHS/family-history-survey.elicit through Admin > Apply Survey Definition.
family.history.survey.key=5e91c606-59a1-450a-a8d7-2f1530ff472b
# The post-survey action FHHS records its uploads against, found by key because its id is
# whatever the import minted (UC-005 BR-004). family.history.upload.psa.id, if set, overrides it.
family.history.upload.psa.key=9f57b644-fecd-557d-a466-07ec19bb95c4
family.history.sftp.enabled=true

# Family History Async Configuration
family.history.async.threads=2

# Application Base URL for PDF generation
app.base.url=${APP_BASE_URL:http://localhost:8080}
quarkus.native.additional-build-args=--initialize-at-run-time=org.apache.batik


# =============================================================================
# %dev
# =============================================================================
%dev.quarkus.http.port=8082
# survey.reports.url rows point back at this app via host.docker.internal (so they also work
# when FHHS runs as a container reaching the host). Quarkus's Host-header validation rejects
# anything but localhost/127.0.0.1/[::1] by default, so allow it explicitly for dev mode.
%dev.quarkus.http.host-validation.allowed-hosts=localhost,host.docker.internal
%dev.pedigree.url=http://localhost:8083/svg
%dev.quarkus.hibernate-orm.log.sql=false

# Datasource: local Postgres (see ../docker-compose.yml at the repo root)
%dev.quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5452/survey
%dev.quarkus.datasource.owner.jdbc.url=jdbc:postgresql://localhost:5452/survey
%dev.quarkus.datasource.dev.jdbc.url=jdbc:postgresql://localhost:5452/survey
%dev.quarkus.datasource.dev.db-kind=postgresql
%dev.quarkus.datasource.dev.db-version=16
%dev.quarkus.datasource.dev.username=elicit_owner
%dev.quarkus.datasource.dev.password=SURVEYPW
%dev.quarkus.datasource.dev.jdbc.transaction-isolation-level=serializable
%dev.quarkus.flyway.owner.locations=db/migration,db/dev

# SFTP Configuration (using SSH key authentication)
%dev.family.history.sftp.host=localhost
%dev.family.history.sftp.username=fhhs_user
%dev.family.history.sftp.path=/upload/reports
%dev.family.history.sftp.port=22
# Choose authentication method:
# Option 1: Password authentication (uncomment for password auth)
#%dev.family.history.sftp.password=pass
# Option 2: SSH Key authentication (comment out for password auth)
%dev.family.history.sftp.privateKey=../sftp_ssh/fhhs_user

# Family History Async Configuration
%dev.family.history.async.threads=1

# Console logging format for development (human-readable)
%dev.quarkus.log.console.format=%d{HH:mm:ss} %-5p [%c{2.}] (%t) %s%e%n
%dev.quarkus.log.console.json=false

# Debug logging for SFTP authentication troubleshooting
%dev.quarkus.log.category."com.elicitsoftware.familyhistory.SftpService".level=DEBUG
%dev.quarkus.log.category."com.jcraft.jsch".level=DEBUG


# =============================================================================
# %test
# =============================================================================
%test.pedigree.url=http://localhost:8083/svg
# Datasource URLs are injected at runtime by PostgresTestResource (Testcontainers) —
# see src/test/resources/application.properties. No external database is required.


# =============================================================================
# %docker (local docker-compose stack; see ../docker-compose.yml at the repo root)
# =============================================================================
%docker.family.history.sftp.enabled=false
%docker.family.history.sftp.host=sftpServer
%docker.family.history.sftp.username=fhhs_user
%docker.family.history.sftp.password=pass
%docker.family.history.sftp.path=/upload/reports
%docker.family.history.sftp.port=22


# =============================================================================
# %prod
# =============================================================================
# SFTP Configuration (using SSH key authentication). Uncomment and configure
# for production use - the defaults above intentionally leave SFTP unconfigured.
#%prod.family.history.sftp.host=your-sftp-server.com
#%prod.family.history.sftp.username=fhhs_user
#%prod.family.history.sftp.privateKey=/path/to/your/private/key
#%prod.family.history.sftp.path=/upload/reports
#%prod.family.history.sftp.port=22
